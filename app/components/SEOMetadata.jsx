'use client'

// This component is no longer needed in Next.js App Router
// Metadata is now handled via layout.tsx and generateMetadata()
// Keeping this file for backwards compatibility but it's not used

export default function SEOMetadata({ children }) {
  return <>{children}</>
}